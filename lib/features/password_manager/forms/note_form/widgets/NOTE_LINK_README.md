# Note Link Button - Ссылки на заметки в Quill Editor

## Описание

Кастомная кнопка для Quill Editor, которая позволяет вставлять ссылки на другие
заметки. При клике на кнопку открывается модальное окно выбора заметки, после
чего вставляется кликабельная ссылка.

## Возможности

✅ Кастомная кнопка в Quill Toolbar ✅ Интеграция с модальным окном выбора
заметок ✅ Вставка ссылок в формате `note://noteId` ✅ Обработка кликов по
ссылкам с предложением открыть заметку ✅ Сохранение названия заметки в тексте
ссылки

## Как работает

### 1. Вставка ссылки

```dart
// При нажатии на кнопку:
1. Открывается модалка выбора заметки (note_picker_modal)
2. Пользователь выбирает заметку
3. В позицию курсора вставляется текст с именем заметки
4. К тексту применяется ссылка формата: note://noteId
```

### 2. Клик по ссылке

```dart
// При клике на ссылку на заметку:
1. Проверяется формат URL (начинается с note://)
2. Извлекается ID заметки
3. Показывается диалог подтверждения
4. При подтверждении открывается связанная заметка
```

## Формат ссылок

Ссылки на заметки используют специальный протокол:

```
note://noteId
```

Где `noteId` - это ID заметки из базы данных.

## Интеграция в Toolbar

В `note_form_screen.dart` кнопка добавлена рядом с стандартными кнопками Quill:

```dart
Row(
  children: [
    QuillSimpleToolbar(...),
    NoteLinkButton(
      controller: _quillController,
      iconSize: 18,
    ),
  ],
)
```

## Кастомизация

### Изменить иконку

```dart
NoteLinkButton(
  controller: _quillController,
  iconSize: 20,  // Размер иконки
  iconTheme: QuillIconTheme(
    iconUnselectedColor: Colors.blue,
    iconUnselectedFillColor: Colors.white,
  ),
)
```

## Компоненты

### NoteLinkButton

Кастомная кнопка для toolbar:

- Открывает модалку выбора заметки
- Вставляет ссылку в документ
- Поддерживает форматирование выделенного текста

### onLaunchUrl handler

В QuillEditorConfig добавлен обработчик:

- Перехватывает клики по ссылкам
- Проверяет формат `note://`
- Показывает диалог открытия заметки

## Сценарии использования

### Создание ссылки на пустом месте

1. Установите курсор в нужное место
2. Нажмите кнопку ссылки на заметку
3. Выберите заметку из списка
4. Текст с именем заметки будет вставлен и отформатирован как ссылка

### Превращение текста в ссылку

1. Выделите существующий текст
2. Нажмите кнопку ссылки на заметку
3. Выберите заметку
4. Выделенный текст станет ссылкой на заметку

### Открытие связанной заметки

1. Кликните на ссылку на заметку в тексте
2. Подтвердите открытие в диалоге
3. Связанная заметка откроется в режиме редактирования

## Технические детали

### LinkAttribute

Используется стандартный `LinkAttribute` из flutter_quill:

```dart
controller.formatText(
  index,
  length,
  LinkAttribute(linkUrl),
);
```

### Навигация

При открытии связанной заметки используется `go_router`:

```dart
context.push('/password-manager/notes/edit/$noteId');
```

## Будущие улучшения

- 🔮 Предпросмотр заметки при наведении на ссылку
- 🔮 Поддержка drag-and-drop заметок из списка
- 🔮 Визуальный индикатор ссылок на заметки (другой цвет/иконка)
- 🔮 Граф связей между заметками
- 🔮 Проверка существования заметки при рендере
